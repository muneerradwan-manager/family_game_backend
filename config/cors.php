<?php

return [

    /*
    |--------------------------------------------------------------------------
    | مسارات CORS
    |--------------------------------------------------------------------------
    |
    | الافتراضي في Laravel يغطي api/* فقط، و`broadcasting/auth` ليس تحته.
    | النتيجة على الويب: كل طلبات REST تنجح بينما توثيق الاشتراك في القناة
    | الحيّة يسقط بصمت في المتصفح — فيعمل التطبيق ظاهرياً وهو أعمى عن كل
    | حدث لحظي. ولذلك نذكره صراحةً هنا.
    |
    | (أندرويد وiOS لا يمرّان بـ CORS أصلاً؛ هذا للويب وحده.)
    |
    */

    'paths' => ['api/*', 'broadcasting/auth'],

    'allowed_methods' => ['*'],

    /*
    | مفتوح للتطوير: منفذ Flutter على الويب يتغيّر مع كل تشغيل.
    | قبل الإطلاق ضع نطاقاتك هنا صراحةً بدل '*'.
    */
    'allowed_origins' => [env('CORS_ALLOWED_ORIGINS', '*')],

    'allowed_origins_patterns' => [],

    // Authorization ليست ضمن ترويسات CORS المسموحة تلقائياً.
    'allowed_headers' => ['*'],

    'exposed_headers' => [],

    'max_age' => 3600,

    // لا نستخدم كوكيز: المصادقة بترويسة Bearer.
    'supports_credentials' => false,

];
